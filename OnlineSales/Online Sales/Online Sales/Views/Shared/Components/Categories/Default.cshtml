@model List<OnlineSalesEntity.Entities.Category>

 <div class="list-group">
    <a href="/product" class="list-group-item list-group-item-action">Tüm Kategoriler</a>

    @foreach (var item in Model)
    {
        <a asp-controller="Product" 
        asp-action="List" 
        asp-route-category="@item.Url" 
        class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == item.Url.ToString()?"active":"")">
            @item.Name
        </a>
    }
</div>   