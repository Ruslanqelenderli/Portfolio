 @model List<Online_Sales.ViewModels.ProductViewModel>
 
 <div class="list-group">
    @foreach (var category in Model)
    {
        <a href="#" class="list-group-item list-group-item-action">@category.Name</a>
    }
</div>   